import React from 'react';

function Button1({ label, onClick, style }) {
    return (
      <button1 className="custom-button1" onClick={onClick} style={style}>
        {label}
      </button1>
    );
  }

  export default Button1